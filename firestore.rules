
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Allow logged-in users to read & write everything
    allow read, write: if request.auth != null;

    // Prevent deletes everywhere, just to avoid client delete mistakes
    match /{document=**} {
      allow delete: if false;
    }
  }
}
